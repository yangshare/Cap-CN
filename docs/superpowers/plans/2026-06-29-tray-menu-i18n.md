# 系统托盘菜单国际化（i18n）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让 Tauri 后端构建的系统托盘菜单随用户语言（en / zh）显示，Rust 内嵌小字典 + 读 store/系统 locale 回退。

**架构：** 在 `tray.rs` 中新增 `TrayLocale` 枚举和 `TrayText` 枚举，`tray_locale()` 函数先读 store 中 `i18n` key 的 `language` 字段，再回退到 `sys_locale` crate 检测系统语言；`tray_text()` 函数返回对应语言的 `&'static str`。所有菜单文案硬编码字面量替换为 `tray_text()` 调用。

**技术栈：** Rust、Tauri、tauri-plugin-store、sys-locale crate

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `apps/desktop/src-tauri/Cargo.toml` | 修改 | 新增 `sys-locale = "0.3"` 依赖 |
| `apps/desktop/src-tauri/src/tray.rs` | 修改 | 新增 `TrayLocale`/`TrayText` 枚举、`tray_locale()`/`tray_text()` 函数；替换所有硬编码菜单文案 |
| `Cargo.lock` | 自动更新 | `cargo check` 后自动更新 |

---

### 任务 1：添加 `sys-locale` 依赖

**文件：**
- 修改：`apps/desktop/src-tauri/Cargo.toml:77`（在 `log = "0.4.20"` 行之后）

- [ ] **步骤 1：在 `Cargo.toml` 添加 `sys-locale` 依赖**

在 `[dependencies]` 段落的 `log = "0.4.20"` 行之后添加：

```toml
sys-locale = "0.3"
```

修改后该区域应为：

```toml
log = "0.4.20"
sys-locale = "0.3"
```

- [ ] **步骤 2：运行 `cargo check` 验证依赖可解析**

运行（PowerShell）：

```powershell
cargo check --manifest-path E:\开源项目\录屏软件\Cap-CN\apps\desktop\src-tauri\Cargo.toml -p cap-desktop 2>&1 | Select-Object -Last 20
```

预期：`Finished` 或 `Compiling` 相关输出，无解析错误。`sys-locale 0.3.x` 已在 `Cargo.lock` 中作为传递依赖存在，不会触发新下载。

- [ ] **步骤 3：Commit**

```bash
git add apps/desktop/src-tauri/Cargo.toml Cargo.lock
git commit -m "feat(i18n): 添加 sys-locale 依赖用于托盘菜单语言检测"
```

---

### 任务 2：新增 `TrayLocale`、`TrayText` 枚举和 `tray_locale()`、`tray_text()` 函数

**文件：**
- 修改：`apps/desktop/src-tauri/src/tray.rs`（在 `use` 块之后、`const` 之前插入，约第 26 行之后）

- [ ] **步骤 1：在 `tray.rs` 顶部 `use` 块末尾添加 `tauri_plugin_store::StoreExt` 导入**

现有 `use` 块末尾（第 25 行 `use tauri_plugin_opener::OpenerExt;` 之后）添加：

```rust
use tauri_plugin_store::StoreExt;
```

说明：`tray_locale()` 需调用 `app.store("store")`，这要求 `StoreExt` trait 在作用域内。参考 `recording_settings.rs:11` 的写法。

- [ ] **步骤 2：在 `tray.rs` 的 `use` 块和 `const` 块之间（第 26 行之后）插入 `TrayLocale` 枚举和 `tray_locale()` 函数**

```rust
#[derive(Clone, Copy, PartialEq)]
enum TrayLocale {
    En,
    Zh,
}

fn tray_locale(app: &AppHandle) -> TrayLocale {
    let explicit = app
        .store("store")
        .ok()
        .and_then(|s| s.get("i18n"))
        .and_then(|v| {
            v.as_object()
                .and_then(|obj| obj.get("language"))
                .and_then(|l| l.as_str())
                .map(str::to_owned)
        });
    if let Some(lang) = explicit {
        return match lang.as_str() {
            "zh" => TrayLocale::Zh,
            _ => TrayLocale::En,
        };
    }

    let is_zh = sys_locale::get_locales()
        .any(|l| l.to_lowercase().starts_with("zh"));
    if is_zh { TrayLocale::Zh } else { TrayLocale::En }
}
```

**说明：**
- `app.store("store")` 对应前端 `Store.load("store")`（`store.ts:26`），store 名称一致。
- `s.get("i18n")` 读取 `i18nSettingsStore = declareStore("i18n")`（`store.ts:7`）写入的 key。
- 返回值为 `serde_json::Value`，需要取出 `.language` 字段，此处用 `.as_object().and_then(|obj| obj.get("language"))` 而非 `v.get("language")`，因为 `serde_json::Value::get` 只在 object 上定义。
- 回退使用 `sys_locale::get_locales()` 匹配 `zh*`，与前端 `detectLocale()`（`i18n/index.ts:46`）逻辑一致。

- [ ] **步骤 3：紧接着插入 `TrayText` 枚举和 `tray_text()` 函数**

```rust
#[derive(Clone, Copy)]
enum TrayText {
    OpenMainWindow,
    RecordDisplay,
    RecordWindow,
    RecordArea,
    ScreenshotDisplay,
    ScreenshotWindow,
    ScreenshotArea,
    TakeScreenshot,
    ImportVideo,
    SelectMode,
    ModeStudio,
    ModeInstant,
    ModeScreenshot,
    Previous,
    NoRecentItems,
    ViewAllRecordings,
    ViewAllScreenshots,
    Settings,
    UploadLogs,
    QuitCap,
    RequestPermissions,
}

fn tray_text(locale: TrayLocale, key: TrayText) -> &'static str {
    use TrayLocale::*;
    use TrayText::*;
    match (locale, key) {
        // English
        (En, OpenMainWindow) => "Open Main Window",
        (En, RecordDisplay) => "Record Display",
        (En, RecordWindow) => "Record Window",
        (En, RecordArea) => "Record Area",
        (En, ScreenshotDisplay) => "Screenshot Display",
        (En, ScreenshotWindow) => "Screenshot Window",
        (En, ScreenshotArea) => "Screenshot Area",
        (En, TakeScreenshot) => "Take a Screenshot",
        (En, ImportVideo) => "Import Video...",
        (En, SelectMode) => "Select Mode",
        (En, ModeStudio) => "Studio",
        (En, ModeInstant) => "Instant",
        (En, ModeScreenshot) => "Screenshot",
        (En, Previous) => "Previous",
        (En, NoRecentItems) => "No recent items",
        (En, ViewAllRecordings) => "View all recordings",
        (En, ViewAllScreenshots) => "View all screenshots",
        (En, Settings) => "Settings",
        (En, UploadLogs) => "Upload Logs",
        (En, QuitCap) => "Quit Cap",
        (En, RequestPermissions) => "Request Permissions",
        // 中文
        (Zh, OpenMainWindow) => "打开主窗口",
        (Zh, RecordDisplay) => "录制显示器",
        (Zh, RecordWindow) => "录制窗口",
        (Zh, RecordArea) => "录制区域",
        (Zh, ScreenshotDisplay) => "截取显示器",
        (Zh, ScreenshotWindow) => "截取窗口",
        (Zh, ScreenshotArea) => "截取区域",
        (Zh, TakeScreenshot) => "截图",
        (Zh, ImportVideo) => "导入视频…",
        (Zh, SelectMode) => "选择模式",
        (Zh, ModeStudio) => "工作室",
        (Zh, ModeInstant) => "即时",
        (Zh, ModeScreenshot) => "截图",
        (Zh, Previous) => "最近项目",
        (Zh, NoRecentItems) => "暂无最近项目",
        (Zh, ViewAllRecordings) => "查看全部录制",
        (Zh, ViewAllScreenshots) => "查看全部截图",
        (Zh, Settings) => "设置",
        (Zh, UploadLogs) => "上传日志",
        (Zh, QuitCap) => "退出 Cap",
        (Zh, RequestPermissions) => "请求权限",
    }
}
```

**说明：**
- `match (locale, key)` 穷举所有 `(TrayLocale, TrayText)` 组合，新增 `TrayText` 变体时编译器会报未覆盖分支。
- 英文文案严格沿用当前硬编码原文。
- 中文文案已由规格确认（规格 §3.3 翻译表）。
- `ImportVideo` 中文使用全角省略号 `…` 替代英文 `...`。

- [ ] **步骤 4：运行 `cargo check` 验证编译**

运行（PowerShell）：

```powershell
cargo check --manifest-path E:\开源项目\录屏软件\Cap-CN\apps\desktop\src-tauri\Cargo.toml -p cap-desktop 2>&1 | Select-Object -Last 5
```

预期：`Finished` 或仅 warnings。此时 `TrayText` 和 `tray_text` 尚没有被调用，可能有 `dead_code` warning，这是正常的。

- [ ] **步骤 5：Commit**

```bash
git add apps/desktop/src-tauri/src/tray.rs
git commit -m "feat(i18n): 新增 TrayLocale/TrayText 枚举和 tray_locale/tray_text 函数"
```

---

### 任务 3：替换 `create_previous_submenu` 中的硬编码文案

**文件：**
- 修改：`apps/desktop/src-tauri/src/tray.rs:273-311`

- [ ] **步骤 1：修改 `create_previous_submenu` 函数签名和内部硬编码文案**

该函数当前代码（第 273-311 行）涉及的硬编码文案有：
- 第 278 行：`"Previous"` — 子菜单名称
- 第 282 行：`"No recent items"` — 空状态项
- 第 289 行：`"Previous"` — 非空子菜单名称

修改方案：在函数开头调用 `tray_locale(app)` 获取 locale，将三处硬编码替换为 `tray_text()` 调用。

将 `create_previous_submenu` 函数修改为：

```rust
fn create_previous_submenu(
    app: &AppHandle,
    cache: &PreviousItemsCache,
) -> tauri::Result<Submenu<tauri::Wry>> {
    let locale = tray_locale(app);

    if cache.items.is_empty() {
        let submenu = Submenu::with_id(app, "previous", tray_text(locale, TrayText::Previous), false)?;
        submenu.append(&MenuItem::with_id(
            app,
            "previous_empty",
            tray_text(locale, TrayText::NoRecentItems),
            false,
            None::<&str>,
        )?)?;
        return Ok(submenu);
    }

    let submenu = Submenu::with_id(app, "previous", tray_text(locale, TrayText::Previous), true)?;

    for item in &cache.items {
        let id = TrayItem::PreviousItem(item.path.to_string_lossy().to_string());
        let title = truncate_title(&item.pretty_name);

        let type_indicator = match &item.item_type {
            PreviousItemType::StudioRecording => "🎬 ",
            PreviousItemType::InstantRecording { .. } => "⚡ ",
            PreviousItemType::Screenshot => "📷 ",
        };
        let display_title = format!("{type_indicator}{title}");

        let icon = item.thumbnail.as_ref().map(|data| {
            Image::new_owned(data.clone(), item.thumbnail_width, item.thumbnail_height)
        });

        let menu_item = IconMenuItem::with_id(app, id, display_title, true, icon, None::<&str>)?;
        submenu.append(&menu_item)?;
    }

    Ok(submenu)
}
```

**保留不动**：
- `"previous"` / `"previous_empty"` — 这些是 MenuId，非显示文本
- `"🎬 "` / `"⚡ "` / `"📷 "` — 类型指示 emoji
- `format!("{type_indicator}{title}")` 中的 `title` 是用户录制名称，非翻译文本

- [ ] **步骤 2：运行 `cargo check` 验证编译**

运行（PowerShell）：

```powershell
cargo check --manifest-path E:\开源项目\录屏软件\Cap-CN\apps\desktop\src-tauri\Cargo.toml -p cap-desktop 2>&1 | Select-Object -Last 5
```

预期：`Finished`，无 error。

- [ ] **步骤 3：Commit**

```bash
git add apps/desktop/src-tauri/src/tray.rs
git commit -m "feat(i18n): 替换 create_previous_submenu 硬编码文案为 tray_text 调用"
```

---

### 任务 4：替换 `create_mode_submenu` 中的硬编码文案

**文件：**
- 修改：`apps/desktop/src-tauri/src/tray.rs:339-367`

- [ ] **步骤 1：修改 `create_mode_submenu` 函数**

该函数当前硬编码文案有：
- 第 342 行：`"Select Mode"` — 子菜单名称
- 第 345 行：`"Studio"` — 模式标签
- 第 346 行：`"Instant"` — 模式标签
- 第 348-350 行：`"Screenshot"` — 模式标签

修改方案：将 labels 数组中的硬编码字符串替换为 `tray_text()` 调用。`"✓ "` 和 `"   "` 前缀保留不动。

将 `create_mode_submenu` 函数修改为：

```rust
fn create_mode_submenu(app: &AppHandle) -> tauri::Result<Submenu<tauri::Wry>> {
    let locale = tray_locale(app);
    let current_mode = get_current_mode(app);

    let submenu = Submenu::with_id(app, "select_mode", tray_text(locale, TrayText::SelectMode), true)?;

    let modes = [
        (TrayItem::ModeStudio, RecordingMode::Studio, tray_text(locale, TrayText::ModeStudio)),
        (TrayItem::ModeInstant, RecordingMode::Instant, tray_text(locale, TrayText::ModeInstant)),
        (TrayItem::ModeScreenshot, RecordingMode::Screenshot, tray_text(locale, TrayText::ModeScreenshot)),
    ];

    for (tray_item, mode, label) in modes {
        let is_selected = current_mode == mode;
        let display_label = if is_selected {
            format!("✓ {label}")
        } else {
            format!("   {label}")
        };

        let menu_item = MenuItem::with_id(app, tray_item, display_label, true, None::<&str>)?;
        submenu.append(&menu_item)?;
    }

    Ok(submenu)
}
```

**保留不动**：
- `"select_mode"` — 是 MenuId，非显示文本
- `format!("✓ {label}")` / `format!("   {label}")` — 对齐/选中符号，label 来自 `tray_text()`

- [ ] **步骤 2：运行 `cargo check` 验证编译**

运行（PowerShell）：

```powershell
cargo check --manifest-path E:\开源项目\录屏软件\Cap-CN\apps\desktop\src-tauri\Cargo.toml -p cap-desktop 2>&1 | Select-Object -Last 5
```

预期：`Finished`，无 error。

- [ ] **步骤 3：Commit**

```bash
git add apps/desktop/src-tauri/src/tray.rs
git commit -m "feat(i18n): 替换 create_mode_submenu 硬编码文案为 tray_text 调用"
```

---

### 任务 5：替换 `build_tray_menu` 中的硬编码文案

**文件：**
- 修改：`apps/desktop/src-tauri/src/tray.rs:369-521`

这是改动最多的函数，涉及约 21 处硬编码文案替换。

- [ ] **步骤 1：在 `build_tray_menu` 函数开头添加 locale 获取**

在 `build_tray_menu` 函数体的最开头（第 369 行 `fn build_tray_menu(...)` 之后的第一行）添加：

```rust
    let locale = tray_locale(app);
```

- [ ] **步骤 2：替换 minimal onboarding 分支的硬编码（第 370-392 行）**

将第 370-392 行替换为：

```rust
    if should_use_minimal_onboarding_tray_menu(app) {
        return Menu::with_items(
            app,
            &[
                &MenuItem::with_id(
                    app,
                    TrayItem::RequestPermissions,
                    tray_text(locale, TrayText::RequestPermissions),
                    true,
                    None::<&str>,
                )?,
                &PredefinedMenuItem::separator(app)?,
                &MenuItem::with_id(
                    app,
                    "version",
                    format!("Cap v{}", env!("CARGO_PKG_VERSION")),
                    false,
                    None::<&str>,
                )?,
                &MenuItem::with_id(app, TrayItem::Quit, tray_text(locale, TrayText::QuitCap), true, None::<&str>)?,
            ],
        );
    }
```

**保留不动**：
- `"version"` — MenuId
- `format!("Cap v{}", env!("CARGO_PKG_VERSION"))` — 品牌前缀

- [ ] **步骤 3：替换主菜单分支的硬编码（第 400 行往后）**

第 400 行之后的 `MenuItem::with_id` 调用中，逐个替换显示文本：

**"Open Main Window"（第 404 行）→ `tray_text(locale, TrayText::OpenMainWindow)`**

```rust
    menu.append(&MenuItem::with_id(
        app,
        TrayItem::OpenCap,
        tray_text(locale, TrayText::OpenMainWindow),
        true,
        None::<&str>,
    )?)?;
```

**screenshot mode 分支 — "Screenshot Display"/"Screenshot Window"/"Screenshot Area"（第 410-430 行）→ `tray_text(locale, TrayText::ScreenshotDisplay)` 等**

```rust
    if is_screenshot_mode {
        menu.append(&MenuItem::with_id(
            app,
            TrayItem::RecordDisplay,
            tray_text(locale, TrayText::ScreenshotDisplay),
            true,
            None::<&str>,
        )?)?;
        menu.append(&MenuItem::with_id(
            app,
            TrayItem::RecordWindow,
            tray_text(locale, TrayText::ScreenshotWindow),
            true,
            None::<&str>,
        )?)?;
        menu.append(&MenuItem::with_id(
            app,
            TrayItem::RecordArea,
            tray_text(locale, TrayText::ScreenshotArea),
            true,
            None::<&str>,
        )?)?;
```

**非 screenshot mode 分支 — "Record Display"/"Record Window"/"Record Area"（第 432-452 行）→ `tray_text(locale, TrayText::RecordDisplay)` 等**

```rust
    } else {
        menu.append(&MenuItem::with_id(
            app,
            TrayItem::RecordDisplay,
            tray_text(locale, TrayText::RecordDisplay),
            true,
            None::<&str>,
        )?)?;
        menu.append(&MenuItem::with_id(
            app,
            TrayItem::RecordWindow,
            tray_text(locale, TrayText::RecordWindow),
            true,
            None::<&str>,
        )?)?;
        menu.append(&MenuItem::with_id(
            app,
            TrayItem::RecordArea,
            tray_text(locale, TrayText::RecordArea),
            true,
            None::<&str>,
        )?)?;
```

**"Take a Screenshot"（第 456 行）→ `tray_text(locale, TrayText::TakeScreenshot)`**

```rust
        menu.append(&MenuItem::with_id(
            app,
            TrayItem::TakeScreenshot,
            tray_text(locale, TrayText::TakeScreenshot),
            true,
            None::<&str>,
        )?)?;
    }
```

**"Import Video..."（第 464 行）→ `tray_text(locale, TrayText::ImportVideo)`**

```rust
    menu.append(&MenuItem::with_id(
        app,
        TrayItem::ImportVideo,
        tray_text(locale, TrayText::ImportVideo),
        true,
        None::<&str>,
    )?)?;
```

**"View all recordings"（第 478 行）→ `tray_text(locale, TrayText::ViewAllRecordings)`**

```rust
    menu.append(&MenuItem::with_id(
        app,
        TrayItem::ViewAllRecordings,
        tray_text(locale, TrayText::ViewAllRecordings),
        true,
        None::<&str>,
    )?)?;
```

**"View all screenshots"（第 484 行）→ `tray_text(locale, TrayText::ViewAllScreenshots)`**

```rust
    menu.append(&MenuItem::with_id(
        app,
        TrayItem::ViewAllScreenshots,
        tray_text(locale, TrayText::ViewAllScreenshots),
        true,
        None::<&str>,
    )?)?;
```

**"Settings"（第 492 行）→ `tray_text(locale, TrayText::Settings)`**

```rust
    menu.append(&MenuItem::with_id(
        app,
        TrayItem::OpenSettings,
        tray_text(locale, TrayText::Settings),
        true,
        None::<&str>,
    )?)?;
```

**"Upload Logs"（第 500 行）→ `tray_text(locale, TrayText::UploadLogs)`**

```rust
    menu.append(&MenuItem::with_id(
        app,
        TrayItem::UploadLogs,
        tray_text(locale, TrayText::UploadLogs),
        true,
        None::<&str>,
    )?)?;
```

**品牌版本行（第 507 行）— 保留不动**

```rust
    menu.append(&MenuItem::with_id(
        app,
        "version",
        format!("Cap v{}", env!("CARGO_PKG_VERSION")),
        false,
        None::<&str>,
    )?)?;
```

**"Quit Cap"（第 514 行）→ `tray_text(locale, TrayText::QuitCap)`**

```rust
    menu.append(&MenuItem::with_id(
        app,
        TrayItem::Quit,
        tray_text(locale, TrayText::QuitCap),
        true,
        None::<&str>,
    )?)?;
```

- [ ] **步骤 4：运行 `cargo check` 验证编译**

运行（PowerShell）：

```powershell
cargo check --manifest-path E:\开源项目\录屏软件\Cap-CN\apps\desktop\src-tauri\Cargo.toml -p cap-desktop 2>&1 | Select-Object -Last 5
```

预期：`Finished`，无 error。

- [ ] **步骤 5：Commit**

```bash
git add apps/desktop/src-tauri/src/tray.rs
git commit -m "feat(i18n): 替换 build_tray_menu 硬编码文案为 tray_text 调用"
```

---

### 任务 6：运行 `cargo check` 全面验证 + `cargo clippy` 检查

**文件：**
- 无新修改，仅验证

- [ ] **步骤 1：运行 `cargo check`**

运行（PowerShell）：

```powershell
cargo check --manifest-path E:\开源项目\录屏软件\Cap-CN\apps\desktop\src-tauri\Cargo.toml -p cap-desktop 2>&1 | Select-Object -Last 10
```

预期：`Finished`，无 error。

- [ ] **步骤 2：运行 `cargo clippy` 检查代码质量**

运行（PowerShell）：

```powershell
cargo clippy --manifest-path E:\开源项目\录屏软件\Cap-CN\apps\desktop\src-tauri\Cargo.toml -p cap-desktop 2>&1 | Select-Object -Last 20
```

预期：无 warning/error 与本次新增代码相关。如 `tray_locale`/`tray_text` 有 `dead_code` warning，说明有未使用的函数，检查是否所有替换都已完成。

- [ ] **步骤 3：如有 clippy 问题，修复后 Commit**

```bash
git add apps/desktop/src-tauri/src/tray.rs
git commit -m "fix(i18n): 修复 clippy 检出的问题"
```

---

### 任务 7：最终验证 — 检查无遗漏硬编码

**文件：**
- 仅检查 `apps/desktop/src-tauri/src/tray.rs`

- [ ] **步骤 1：搜索 `tray.rs` 中残留的硬编码英文字面量**

使用 Grep 工具（而非 shell `grep`）：

- `pattern`: `"[A-Z][a-z]+ `
- `path`: `apps/desktop/src-tauri/src/tray.rs`
- `output_mode`: `content`

预期：不应再有任何以大写字母开头的英文菜单文案字面量（如 `"Open Main Window"`、`"Record Display"` 等），只应保留：
- MenuId（小写加下划线，如 `"open_cap"`、`"previous"`、`"select_mode"`、`"version"`）；
- 品牌字符串 `"Cap v"`（`format!("Cap v{}", ...)`）—— 预期会命中，属正常；
- emoji/对齐前缀（`"✓ "`、`"   "`、`"🎬 "` 等，不含字母，不会被本 pattern 命中）。

若命中其他以大写字母开头的英文短语，说明有遗漏未替换的硬编码文案。

- [ ] **步骤 2：验证 `TrayText` 枚举与规格翻译表的一致性**

逐条对照规格 §3.3 翻译表，确认 20 个 `TrayText` 变体（`OpenMainWindow` 到 `RequestPermissions`）均有对应的 en/zh match 分支：

| TrayText 变体 | en | zh |
|---|---|---|
| OpenMainWindow | Open Main Window | 打开主窗口 |
| RecordDisplay | Record Display | 录制显示器 |
| RecordWindow | Record Window | 录制窗口 |
| RecordArea | Record Area | 录制区域 |
| ScreenshotDisplay | Screenshot Display | 截取显示器 |
| ScreenshotWindow | Screenshot Window | 截取窗口 |
| ScreenshotArea | Screenshot Area | 截取区域 |
| TakeScreenshot | Take a Screenshot | 截图 |
| ImportVideo | Import Video... | 导入视频… |
| SelectMode | Select Mode | 选择模式 |
| ModeStudio | Studio | 工作室 |
| ModeInstant | Instant | 即时 |
| ModeScreenshot | Screenshot | 截图 |
| Previous | Previous | 最近项目 |
| NoRecentItems | No recent items | 暂无最近项目 |
| ViewAllRecordings | View all recordings | 查看全部录制 |
| ViewAllScreenshots | View all screenshots | 查看全部截图 |
| Settings | Settings | 设置 |
| UploadLogs | Upload Logs | 上传日志 |
| QuitCap | Quit Cap | 退出 Cap |
| RequestPermissions | Request Permissions | 请求权限 |

预期：全部匹配，无遗漏。

- [ ] **步骤 3：验证 `TrayItem` 枚举、`MenuId` 映射、事件处理逻辑均未改动**

按文件名固定比较（不依赖提交数，避免任务 6/7 产生修复提交后 `HEAD~N` 偏移），找出本分支首次改动 `tray.rs` 的提交作为基准：

```powershell
git log --oneline -- apps/desktop/src-tauri/src/tray.rs
```

取列表中**最早一个由本计划产生的提交**作为 `<base>`，然后人工 review 该提交至今的 `tray.rs` 差异，确认 `TrayItem` 枚举定义、`From<TrayItem> for MenuId` / `TryFrom<MenuId> for TrayItem`、`on_menu_event` 闭包内的 match 分支均**未被修改或删除**，diff 中这些区域只应出现因周围变换引起的上下文行。

```powershell
git diff <base> -- apps/desktop/src-tauri/src/tray.rs
```

预期：`TrayItem`/`MenuId`/`on_menu_event` 相关定义无实质改动，仅有文案字面量被替换为 `tray_text()` 调用及插入的新枚举/函数。

- [ ] **步骤 4：Commit（如有修复）**

如果有发现遗漏并修复：

```bash
git add apps/desktop/src-tauri/src/tray.rs
git commit -m "fix(i18n): 修复遗漏的硬编码菜单文案"
```

---

## 自检

### 1. 规格覆盖度

| 规格章节 | 对应任务 | 状态 |
|---------|---------|------|
| §3.1 依赖 | 任务 1 | ✅ |
| §3.2 locale 解析 | 任务 2 步骤 2 | ✅ |
| §3.3 翻译表 | 任务 2 步骤 3 | ✅ |
| §3.4 替换硬编码 — build_tray_menu | 任务 5 | ✅ |
| §3.4 替换硬编码 — create_mode_submenu | 任务 4 | ✅ |
| §3.4 替换硬编码 — create_previous_submenu | 任务 3 | ✅ |
| §3.5 刷新时机（不改） | 不涉及 | ✅ |
| §四 验证 — 编译 | 任务 6 | ✅ |
| §五 范围边界 — 不动前端 | 全部任务 | ✅ |
| §五 范围边界 — 不动 TrayItem/MenuId/事件逻辑 | 任务 7 步骤 3 | ✅ |

### 2. 占位符扫描

- 无 "待定"、"TODO"、"后续实现" 等占位符
- 所有代码步骤均包含完整代码
- 所有测试步骤均有明确的运行命令和预期输出

### 3. 类型一致性

- `TrayLocale`：枚举，`En` / `Zh` 变体，在任务 2 定义，在任务 3/4/5 使用 — 一致 ✅
- `TrayText`：枚举，20 个变体，在任务 2 定义，在任务 3/4/5 使用 — 一致 ✅
- `tray_locale(app: &AppHandle) -> TrayLocale`：在任务 2 定义，在任务 3/4/5 使用 — 一致 ✅
- `tray_text(locale: TrayLocale, key: TrayText) -> &'static str`：在任务 2 定义，在任务 3/4/5 使用 — 一致 ✅
- `app.store("store")` 依赖 `StoreExt` trait 导入（`use tauri_plugin_store::StoreExt`），在任务 2 步骤 1 添加 — 一致 ✅
